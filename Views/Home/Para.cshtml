<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Message</title>

  <link rel="stylesheet" href="./style.css" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
      background-size: 400% 400%;
      background-attachment: fixed;
      background-position: center;
      background-repeat: repeat;
      background-blend-mode: overlay;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding: 20px;
      overflow-y: auto;
      position: relative;
    }

    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 20% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
      pointer-events: none;
    }

    .container {
      background: rgba(255, 255, 255, 0.98);
      border-radius: 30px;
      padding: 40px 35px;
      box-shadow: 0 30px 80px rgba(0, 0, 0, 0.3),
                  0 0 40px rgba(255, 20, 147, 0.3);
      max-width: 650px;
      text-align: center;
      backdrop-filter: blur(10px);
      position: relative;
      z-index: 1;
      border: 3px solid rgba(255, 20, 147, 0.3);
      max-height: 80vh;
      overflow-y: auto;
    }

    h1 {
      font-size: 2.5em;
      background: linear-gradient(135deg, #ff1493 0%, #ff69b4 50%, #ff1493 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 25px;
      font-weight: 800;
      letter-spacing: 2px;
    }

    .paragraph-content {
      text-align: justify;
      line-height: 1.8;
      font-size: 1.1em;
      color: #333;
      margin: 20px 0;
      word-wrap: break-word;
      padding: 15px;
      background: rgba(255, 105, 180, 0.05);
      border-radius: 15px;
      border-left: 4px solid rgba(255, 20, 147, 0.3);
    }

    .celebration {
      font-size: 2.5em;
      margin-top: 25px;
      text-align: center;
    }

    .hearts {
      display: flex;
      justify-content: center;
      gap: 15px;
      margin-top: 20px;
      font-size: 2em;
    }

    .heart {
      filter: drop-shadow(0 4px 8px rgba(255, 20, 147, 0.3));
    }

    ::-webkit-scrollbar {
      width: 8px;
    }

    ::-webkit-scrollbar-track {
      background: rgba(255, 20, 147, 0.1);
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb {
      background: rgba(255, 20, 147, 0.3);
      border-radius: 10px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: rgba(255, 20, 147, 0.5);
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>A Special Message 💌</h1>
    
    <div class="paragraph-content">
      fnwofnwiecnwicnwiwicwiucnwiabvfinwiunfiuwnfwufnfwfunfwuunwwncwencwcnwicnwifunwrifuwfuiwiuwiuchwfuiwhciwcwicnwicnwifbrirwhlwinwiualurynwelryweinrynwepytpwnytpwntywptnywpuiwpnfiwefyenieufhpnifuhrpneurhpeereh
    </div>

    <div class="celebration">🎉</div>
    <div class="hearts">
      <span class="heart">💕</span>
      <span class="heart">💖</span>
      <span class="heart">💗</span>
    </div>
  </div>

  <script>
    // Create confetti effect
    function createConfetti() {
      for (let i = 0; i < 50; i++) {
        const confetti = document.createElement('div');
        confetti.style.position = 'fixed';
        confetti.style.left = Math.random() * window.innerWidth + 'px';
        confetti.style.top = '-10px';
        confetti.style.width = Math.random() * 15 + 'px';
        confetti.style.height = confetti.style.width;
        confetti.style.background = ['#ff1493', '#ff69b4', '#ffb6d9', '#ffc0cb', '#ff20b2'][Math.floor(Math.random() * 5)];
        confetti.style.borderRadius = '50%';
        confetti.style.pointerEvents = 'none';
        document.body.appendChild(confetti);
        setTimeout(() => confetti.remove(), 5000);
      }
    }

    // Create confetti on page load
    createConfetti();

    // Create more confetti periodically
    setInterval(createConfetti, 3000);

    // Add sparkles on random positions
    setInterval(() => {
      const sparkle = document.createElement('div');
      sparkle.textContent = '✨';
      sparkle.style.position = 'fixed';
      sparkle.style.left = Math.random() * window.innerWidth + 'px';
      sparkle.style.top = Math.random() * window.innerHeight + 'px';
      sparkle.style.fontSize = '1.5em';
      sparkle.style.pointerEvents = 'none';
      document.body.appendChild(sparkle);
      setTimeout(() => sparkle.remove(), 1000);
    }, 500);
  </script>
</body>

</html>
