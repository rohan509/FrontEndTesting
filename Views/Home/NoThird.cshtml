<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ask Her Out</title>

  <link rel="stylesheet" href="./style.css" />
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      overflow: hidden;
    }

    body::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 20% 50%, rgba(255, 255, 255, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
      pointer-events: none;
    }

    .container {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 25px;
      padding: 25px 20px;
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.25);
      max-width: 500px;
      text-align: center;
      backdrop-filter: blur(10px);
      position: relative;
      z-index: 1;
      border: 2px solid rgba(255, 255, 255, 0.5);
    }

    .tenor-gif-embed {
      margin-bottom: 15px;
      border-radius: 15px;
      overflow: hidden;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      border: 2px solid rgba(245, 87, 108, 0.2);
    }

    h1 {
      font-size: 2em;
      background: linear-gradient(135deg, #f5576c 0%, #f093fb 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 8px;
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    p {
      font-size: 1em;
      color: #666;
      margin-bottom: 25px;
      font-weight: 500;
    }

    .btn {
      display: flex;
      gap: 30px;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn div {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
      position: relative;
    }

    .btn div::before {
      content: '';
      position: absolute;
      top: -15px;
      left: 50%;
      transform: translateX(-50%);
      width: 0;
      height: 3px;
      background: linear-gradient(90deg, transparent, #f5576c, transparent);
      transition: width 0.3s ease;
    }

    .btn div:hover::before {
      width: 80%;
    }

    .btn a {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 10px;
      padding: 0;
      background: none;
      color: #333;
      text-decoration: none;
      font-size: 1.1em;
      font-weight: bold;
      transition: all 0.3s ease;
      position: relative;
    }

    .btn a::after {
      content: '';
      position: absolute;
      bottom: -5px;
      left: 0;
      right: 0;
      height: 3px;
      background: linear-gradient(90deg, #f5576c, #f093fb);
      transform: scaleX(0);
      transition: transform 0.3s ease;
    }

    .btn a:hover::after {
      transform: scaleX(1);
    }

    .btn img {
      border-radius: 15px;
      transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
      border: 3px solid rgba(245, 87, 108, 0.1);
      position: relative;
    }

    .btn img:hover {
      transform: scale(1.15) translateY(-10px);
      box-shadow: 0 20px 50px rgba(245, 87, 108, 0.3);
      border: 3px solid rgba(245, 87, 108, 0.3);
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="tenor-gif-embed" data-postid="15974530976611222074" data-share-method="host" data-aspect-ratio="1.26923"
      data-width="100%">
      <a href="https://tenor.com/view/peach-goma-phone-gif-15974530976611222074">Peach Goma Phone Sticker</a>from
      <a href="https://tenor.com/search/peach+goma-stickers">Peach Goma Stickers</a>
    </div>
    <script type="text/javascript" async src="https://tenor.com/embed.js"></script>

    <h1>Baby Man jao na! Kitna bhav khaogi 😭</h1>
    <p>bhut glt baat hai yrr😭</p>

    <div class="btn">
      <div>
        <a asp-controller="Home" asp-action="Yes">
          Yes
          <img src="/images/yes.jpeg" alt="Yes" style="width: 100px; height: 100px;" />
        </a>
      </div>
      <div>
        <a asp-controller="Home" asp-action="NoThird" id="move-random">
          No
          <img src="/images/download.jpeg" alt="No" style="width: 100px; height: 100px;" />
        </a>
      </div>
    </div>
  </div>

  <script src=".wwwroot/js/site.js"></script>
  <script>
    const noButton = document.getElementById('move-random');
    const noImage = noButton.querySelector('img');
    
    function moveButtonRandomly(e) {
      e.preventDefault();
      
      const randomX = Math.random() * (window.innerWidth - 200) - (window.innerWidth / 2) + 100;
      const randomY = Math.random() * (window.innerHeight - 200) - (window.innerHeight / 2) + 100;
      
      noButton.style.position = 'fixed';
      noButton.style.left = randomX + 'px';
      noButton.style.top = randomY + 'px';
      noButton.style.transition = 'all 0.3s ease';
    }
    
    noButton.addEventListener('mouseover', moveButtonRandomly);
    noImage.addEventListener('mouseover', moveButtonRandomly);
  </script>
</body>

</html>